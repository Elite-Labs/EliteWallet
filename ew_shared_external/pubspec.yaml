name: ew_shared_external
description: Shared external libraries for monero and haven
version: 0.0.1
author: Elite Wallet
homepage: https://elitewallet.sc

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: sc.elitewallet.ew_shared_external
        pluginClass: EwSharedExternalPlugin
      ios:
        pluginClass: EwSharedExternalPlugin